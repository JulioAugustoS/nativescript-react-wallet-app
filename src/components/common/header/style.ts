import { StyleSheet } from "react-nativescript";

export const style = StyleSheet.create({
  container: {
    width: "100%",
    backgroundColor: "#19173D",
    paddingLeft: 16,
    paddingRight: 16,
  },
});
